from __future__ import annotations

from fastapi import APIRouter

# Placeholder from removed routes/future.py; mount in router.py when endpoints exist.
router = APIRouter(prefix="/modules", tags=["future-modules"])
